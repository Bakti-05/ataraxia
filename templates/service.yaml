apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.custName }}-svc
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: {{ .Values.service.selector.app }}
