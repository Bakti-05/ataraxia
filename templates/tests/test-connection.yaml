apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ataraxia.fullname" . }}-test-connection"
  labels:
    {{- include "ataraxia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ataraxia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
